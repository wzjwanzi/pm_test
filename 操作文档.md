# 必备技能
## 基站方面
1、ssh 登录
2、适当时候敲命令：odi -n duapp0 display-ue-info |grep Crnti |awk '{print $3}'  |xargs -I {} sh -c "odi -n duapp0 release-ue {}"
3、适当时候敲命令：odi -n duapp0 display-ue-info |grep Super | awk '{print $3}' |xargs -I {} sh -c "odi -n duapp0 force-rlc-escape-ctrl 1 {}"

### 基站日志收集
1. PM统计周期15分钟；
google浏览器web操作
2. 基站日志:192.168.13.236:8400   root/5GNR@root,点击调试信息，点击下载，选中日志文件，下载到C:\Users\AdminStratior\Downloads、需要转移到D:\test\autopm_system\log
3.入网信令：192.168.13.236:8400   root/5GNR@root，点击调试信息，点击抓包，选中本地抓包，勾选信令面，点击开始抓包（pm周期开始前抓取，一直抓取到pm周期结束后）

b.收集每秒的odi打印，odi -n duapp0 dump-rlc-om-info， odi -n duapp0 display-mac-non-zero-om 1和odi -n duapp0 display-mac-non-zero-om 2 odi -n upapp net-stat
命令如下，测试前提前执行：
while true; do odi -n duapp0 dump-rlc-om-info；do odi -n duapp0 display-mac-non-zero-om 1；do odi -n duapp0 display-mac-non-zero-om 2;odi -n upapp net-stat;date; sleep 1; done
c.需要收小区速率日志：numOfDuapp=`ps -ef |grep mac_phy_intf |grep duapp | wc -l`; while true; do clear; for ((i=0;i<$numOfDuapp;i++)); do odi -q -n duapp$i show-mac-throughput-count 5;done;date; sleep 2; done; \r
d.cpu占用日志

## 灌包服务器
1、ssh 登录
2、下行灌包命令:iperf -u -c 10.6.250.12 -i 1 -t 60000 -b 250m -l 1300 -p 6011 -P 1
3、上行收包命令:iperf -s -i 1 -p 7011
4、ping包命令:ping 10.6.250.1

## 控制手机操作方面
1、控制手机使用adb shell ping：ping 10.88.149.164
2、控制手机使用adb shell iperf：-u -c 10.88.149.164 -i 1 -t 9999 -b 100 -l 1350 -p 7011

